query pageOrg($input: PageOrgInput){
    pageOrg(input: $input){
        pageCurrent
        pageSize
        records{
            disable
            key
            name
        }
        totalRecords
    }
}