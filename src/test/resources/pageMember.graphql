query pageMember($input: MemberPageInput){
    pageMember(input: $input){
        pageCurrent
        pageSize
        records{
            id
            name
            phone
            roleIds
            roleNames
        }
        totalRecords
    }
}